{
  "title": "SEO Headings Structure Analyzer Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "The homepage URL to start crawling from",
      "pattern": "^https?://.*",
      "prefill": "https://mysmartdigital.fr/",
      "editor": "textfield"
    },
    "maxPages": {
      "title": "Maximum Pages to Check",
      "type": "integer",
      "description": "Maximum number of pages to crawl and analyze for headings",
      "minimum": 1,
      "maximum": 50,
      "default": 2,
      "editor": "number"
    },
    "userAgent": {
      "title": "User Agent",
      "type": "string",
      "description": "User agent string to use for requests",
      "default": "Mozilla/5.0 (compatible; SEO-Headings-Analyzer/1.0)",
      "editor": "textfield"
    },
    "timeout": {
      "title": "Request Timeout",
      "type": "integer",
      "description": "Timeout for page requests (in milliseconds)",
      "minimum": 1000,
      "maximum": 30000,
      "default": 10000,
      "editor": "number"
    },
    "maxRedirects": {
      "title": "Max Redirects",
      "type": "integer",
      "description": "Maximum number of redirects to follow",
      "minimum": 0,
      "maximum": 10,
      "default": 5,
      "editor": "number"
    },
    "includeHeadingText": {
      "title": "Include Heading Text",
      "type": "boolean",
      "description": "Whether to include the actual text content of headings",
      "default": true,
      "editor": "checkbox"
    },
    "includeHeadingStructure": {
      "title": "Include Heading Structure",
      "type": "boolean",
      "description": "Whether to include detailed heading structure analysis",
      "default": true,
      "editor": "checkbox"
    },
    "includeHeadingScore": {
      "title": "Include Heading Score",
      "type": "boolean",
      "description": "Whether to calculate and include heading scores",
      "default": true,
      "editor": "checkbox"
    }
  },
  "required": ["startUrl", "maxPages"]
}
